const fs = require('fs');

const SETTINGSPATH = "./config.json"; //TODO: This should go in %APPDATA%/Roaming when finished, A way to configure this would also be pretty neat, but I'm not sure how that would work.

function login() {
  
}

function getConfig() {
  
}

async function main() {
  let config = getConfig(); // get user config
  let running= 1;
  while (running) {
    break;
  }
}

main();
